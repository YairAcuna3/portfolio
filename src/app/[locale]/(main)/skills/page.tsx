'use client';

import Section from "@/components/Section";

export default function Name() {
    return (
        <Section content={
            <div>
                <h1>Las habilidades que voy aprendiendo</h1>
            </div>
        } />
    );
}